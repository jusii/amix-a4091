# Large disk images — never commit (650 MB each)
hdf/
*.hdf
*.hdf.*

# Scratch: logs, screenshots, pulled Amix binaries, tape extracts, build output
tmp/
build/

# Upstream A4091 FOSS repo — cloned for reference, re-cloneable, has its own .git
reference/a4091-software/

# OS / editor cruft
core
*.core
*.swp
*~
.DS_Store

# Local-only working docs and tooling config — kept on disk, not part of the
# public release (lab notebook, handoffs, Claude Code project config).
NOTES.md
PROPOSAL.md
GRIMOIRE-HANDOFF.md
NEXT-PROJECT-HANDOFF.md
CLAUDE.md
.claude/
